#include<bits/stdc++.h>
using namespace std;

int main()
{
    char str[40];

    cout<<"Enter string:";
    fgets(str,40,stdin);
    cout<<str;
    // cout<<str[5]; //success.
    puts(str); //also displays string.
    return 0;
}